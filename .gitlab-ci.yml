stages: # Ici on déclare toutes nos étapes
  - build
  - deploy

job:build:
  stage: build 
  script:
    - sudo docker-compose down
    - mkdir -p ./database/data
    - sudo docker-compose build
  only:
    - master

job:deploy:
  stage: deploy 
  script: 
    - sudo docker-compose up -d
    - sudo chown -R gitlab-runner ./database && sudo chmod 775 -R ./database
  only:
    - master
